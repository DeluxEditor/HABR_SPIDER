def addLinkRef(urlFrom, urlTo, linkText):
    """
    cur = self.conection.execute("SELECT %s FROM %s WHERE url='%s'" % (urlFrom, urlList, urlTo))
    link = cur.fetchone()
    if link == None:
        cur = self.conection.execute("INSERT INTO %s (%s) VALUES ('%s')" % (linkBetweenURL, fieldName, value))
        return cur.lastrowid
    else:
        return res[0]
    urlTo = getEntryId(self, 'urllist', 'url', url)
    self.cursor.execute("INSERT INTO linkbetwenurl (?) VALUES(?)", (,))"""
    pass